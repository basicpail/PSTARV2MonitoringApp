<UserControl x:Class="PSTARV2MonitoringApp.Views.Dialogs.IdFilterDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:viewmodels="clr-namespace:PSTARV2MonitoringApp.ViewModels.Dialogs"
             d:DataContext="{d:DesignInstance Type=viewmodels:IdFilterDialogViewModel}"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             mc:Ignorable="d">

    <Grid Margin="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="필터링할 ID를 선택하세요." 
                   FontSize="16" 
                   FontWeight="SemiBold"
                   Foreground="White"
                   Margin="0,0,0,15"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="IdSelectionPanel">
                <!-- All 옵션 -->
                <RadioButton x:Name="AllRadioButton" 
                           Content="전체 보기" 
                           GroupName="IdFilter"
                           Tag="ALL"
                           IsChecked="True"
                           FontSize="14"
                           Foreground="White"
                           Margin="0,5"/>

                <!-- 동적으로 ID 라디오 버튼들이 추가됩니다 -->
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>