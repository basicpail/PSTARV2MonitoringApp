<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <SolidColorBrush x:Key="Bg"         Color="#0B0D11"/>
    <SolidColorBrush x:Key="CardBg"     Color="#161A20"/>
    <SolidColorBrush x:Key="CardStroke" Color="#272D36"/>
    <SolidColorBrush x:Key="TextMain"   Color="#F2F4F8"/>
    <SolidColorBrush x:Key="TextSub"    Color="#AEB4BF"/>
    <SolidColorBrush x:Key="Accent"     Color="#B9C4D1"/>
    <SolidColorBrush x:Key="DataGridBg" Color="#1B2028"/>
    <SolidColorBrush x:Key="AlternatingRowBackground" Color="#171C23"/>


    <!-- Generic Card -->
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBg}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CardStroke}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="6"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#000000" Opacity="0.7" BlurRadius="16" ShadowDepth="0"/>
            </Setter.Value>
        </Setter>
    </Style>



    <!-- 헤더 템플릿 -->
    <DataTemplate x:Key="CardHeaderTemplate">
        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <!-- 아이콘 캡슐 -->
                <Border Margin="0,0,10,0">
                    <TextBlock
                FontFamily="Segoe MDL2 Assets"
                FontSize="16" Foreground="White"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Text="{Binding RelativeSource={RelativeSource AncestorType=ContentPresenter}, Path=Uid}"/>
                </Border>

                <!-- 제목 + 추가 텍스트 -->
                <TextBlock Text="{Binding}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextMain}">
        <Run Text="{Binding RelativeSource={RelativeSource AncestorType=ContentPresenter}, Path=ToolTip}"/>
                </TextBlock>
            </StackPanel>

            <!-- 오른쪽 보조 텍스트(단위 등) -->
            <TextBlock Grid.Column="1"
           Text="{Binding RelativeSource={RelativeSource AncestorType=ContentPresenter}, Path=Tag}"
           Foreground="{StaticResource TextSub}" Margin="0,0,8,0"/>
        </Grid>
    </DataTemplate>


    <!-- DataGrid (좌상단 표) -->
    <Style x:Key="CleanDataGrid" TargetType="{x:Type DataGrid}">
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="RowHeight" Value="28"/>
        <Setter Property="ColumnHeaderHeight" Value="32"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource CardStroke}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource CardStroke}"/>
        <Setter Property="EnableRowVirtualization" Value="True"/>
        <Setter Property="EnableColumnVirtualization" Value="True"/>
        <Setter Property="BorderThickness" Value="0,0,0,2"/>

        <Setter Property="CellStyle">
            <Setter.Value>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="TextBlock.TextTrimming" Value="CharacterEllipsis"/>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="ColumnHeaderStyle">
            <Setter.Value>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Foreground" Value="#C3CAD6"/>
                    <Setter Property="Background" Value="{StaticResource CardBg}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource CardStroke}"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- DataGrid Row 시인성/선택 강조 -->
    <Style x:Key="ElevatedRow" TargetType="DataGridRow">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Margin" Value="0,2,0,2"/>
        <Setter Property="Background" Value="{StaticResource DataGridBg}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CardStroke}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#000000" Opacity="0.25" BlurRadius="6" ShadowDepth="0"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridRow">
                    <Border Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="6">
                        <DataGridCellsPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="#2B3543"/>
                <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
                <Setter Property="BorderThickness" Value="1.2"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#25303D"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- 배지(Pill) 베이스: OFF 기본, ON일 때 트리거로 색 바꿈 -->
    <Style x:Key="PillBase" TargetType="Border">
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="6,2"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="#242B35"/>
        <Setter Property="BorderBrush" Value="{StaticResource CardStroke}"/>
    </Style>

    <!--  DataGrid 헤더 컴팩트 + 굵기 -->
    <Style x:Key="StrongHeader" TargetType="DataGridColumnHeader">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="4,3"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="#C3CAD6"/>
        <Setter Property="Background" Value="{StaticResource CardBg}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CardStroke}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>

    <!-- DeviceStatus 카드 내부 행(아이콘 + 라벨 + 값) -->
    <Style x:Key="InfoRow" TargetType="StackPanel">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,6,0,0"/>
    </Style>

    <Style x:Key="Caption" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextSub}"/>
        <Setter Property="FontSize" Value="18"/>
    </Style>

    <!-- 값 텍스트: 길면 줄바꿈 없이 말줄임, 색 통일 -->
    <Style x:Key="InfoValue" TargetType="TextBlock" BasedOn="{StaticResource Caption}">
        <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
        <Setter Property="Margin" Value="8,0,0,0"/>
        <Setter Property="TextWrapping" Value="NoWrap"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <Style x:Key="InfoBorder" TargetType="Border">
        <Setter Property="Width" Value="4"/>
        <Setter Property="Height" Value="18"/>
        <Setter Property="CornerRadius" Value="2"/>
        <Setter Property="Background" Value="#60CDFF"/>
        <Setter Property="Margin" Value="0,0,10,0"/>
    </Style>

    <Style x:Key="InfoEllipse" TargetType="Ellipse">
        <Setter Property="Width" Value="8"/>
        <Setter Property="Height" Value="8"/>
        <Setter Property="Margin" Value="0,0,10,0"/>
    </Style>
</ResourceDictionary>